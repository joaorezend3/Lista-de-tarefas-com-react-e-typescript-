import styled from "styled-components";

export const container = styled.div(() => (`
ul{
display: flex;
text-aling:center;
justify-content:center;
background-color:#;
gap:15px;
}
svg{
color: #fff;
}
li{
    list-style: none;
}
svg:hover{
transform:scale(1.1);
color: #CDFCF6;
}
`
));